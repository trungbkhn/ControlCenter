<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:ignore="SpUsage">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/imageFavorite"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:scaleType="centerCrop"
            app:layout_constraintBottom_toBottomOf="@+id/backgroundSelect"
            app:layout_constraintEnd_toEndOf="@+id/backgroundSelect"
            app:layout_constraintStart_toStartOf="@+id/backgroundSelect"
            app:layout_constraintTop_toTopOf="@+id/backgroundSelect"
            app:shapeAppearanceOverlay="@style/RoundedCornerImage_8" />

        <View
            android:id="@+id/backgroundSelect"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginHorizontal="10dp"
            android:layout_marginVertical="12dp"
            android:background="@drawable/custom_stroke_background"
            android:outlineProvider="none"
            android:visibility="invisible"
            app:layout_constraintDimensionRatio="101:218"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="9dp"
            android:fontFamily="@font/sf_pro_text_regular"
            android:gravity="center"
            android:lineSpacingMultiplier="1.6"
            android:textColor="@color/black"
            android:textSize="12dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/imageFavorite" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>